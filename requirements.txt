requests>=2.28.0
Django>=3.2,<5.0
celery>=5.2.0
celery_progress
redis>=4.0.0
pycountry>=22.3.5
shodan>=1.28.0
bs4
pynmea2
maxminddb
xmltodict
python-libnmap
lxml
pandas>=1.4.0
simplekml>=1.3.6
ipwhois>=1.2.0
PyYAML>=6.0
wappalyzer
# 3D globe rendering deps are intentionally kept in requirements-3d.txt
# to avoid pulling in heavy VTK/Qt6 libraries in headless CI environments.
# Install separately: pip install -r requirements-3d.txt
# Screenshot capture (requires Chrome/Chromium and ChromeDriver installed on system,
# or Selenium 4.6+ which includes selenium-manager to auto-manage drivers)
selenium
